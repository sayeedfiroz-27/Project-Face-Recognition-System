# Trainer Folder

This folder stores trained model files generated by `python3 src/02_train_model.py`.

Generated files:

```text
trainer/face_model.yml
trainer/labels.txt
```

`face_model.yml` stores the trained LBPH face recognition model.

`labels.txt` stores user ID and user name mapping, for example `1,Rahul`.
