# Attendance Folder

This folder stores attendance CSV files generated by `python3 src/03_recognize_and_attendance.py`.

Example generated file:

```text
attendance/attendance_2026-07-30.csv
```

Each row contains user ID, name, and recognition time.
